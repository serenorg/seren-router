//! Reserved Seren identity-token boundary for post-cutover hardening.
//!
//! `crate::gateway_auth` implements the current static Gateway-key contract.
//! The generic template's SerenCore dependencies are intentionally not carried
//! into this single-caller service until identity verification is authorized.
