# Server
BIND_ADDRESS=0.0.0.0:8000

# Router
# Required. Inject the actual value from the deployment secret manager.
SEREN_ROUTER_GATEWAY_KEY=
# Optional. When set, this distinct credential is bound to the beta provider profile.
SEREN_ROUTER_BETA_GATEWAY_KEY=
SEREN_ROUTER_SIDECAR_URL=http://127.0.0.1:4000
SEREN_ROUTER_SIDECAR_READINESS_URL=http://127.0.0.1:19001/healthz/ready
SEREN_ROUTER_REGISTRY_PATH=registry/providers.yaml
# Required policy values. Set reviewed deployment-specific values.
SEREN_ROUTER_COMBINED_PRICE_CEILING_PER_MTOK=
SEREN_ROUTER_HYSTERESIS_FRACTION=
SEREN_ROUTER_MAX_SHARE=
SEREN_ROUTER_SHARE_WINDOW=

# Provider credentials
# Required when the checked-in OpenRouter fallback provider is enabled.
SEREN_ROUTER_KEY_OPENROUTER=
# Required when the checked-in Modal route is enabled.
# Keep this provider credential out of the seren-router application container.
SEREN_ROUTER_KEY_MODAL=
# Required when the checked-in DeepInfra beta route is enabled.
# Store only a model-scoped, spend-limited credential in the deployment secret manager.
SEREN_ROUTER_KEY_DEEPINFRA=

# Logging
# JSON logging is auto-enabled in Kubernetes (KUBERNETES_SERVICE_HOST detected)
RUST_LOG=seren_router=debug,tower_http=debug

# Database
# Required. Startup creates a lazy pool and retries migrations in the background.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/seren_router
